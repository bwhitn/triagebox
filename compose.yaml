services:
  v86-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${V86_PORT:-8000}:8000"
    volumes:
      - ./public:/srv/public:ro
      - ./public/uploads:/srv/public/uploads:rw
