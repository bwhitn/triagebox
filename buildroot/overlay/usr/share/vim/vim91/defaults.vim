" Minimal defaults.vim for Buildroot images without the full Vim runtime.
" Vim sources this before /etc/vimrc, so keep it self-contained.
let skip_defaults_vim = 1
set nocompatible
set backspace=indent,eol,start
set history=50
set ruler
